@model List<SiGe.SerieViewModel>

@{ ViewData["Title"] = "Serie"; }

<div class="page-header">
    <div class="row">
        <div class="col-md-6 col-sm-12">
            <div class="title">
                <h4>Series</h4>
            </div>
            <nav aria-label="breadcrumb" role="navigation">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="index.html">Series</a></li>
                    <li class="breadcrumb-item active" aria-current="page">Lista del Series por Sucursal</li>
                </ol>
            </nav>
        </div>
    </div>
</div>

<p>
    <a type="button" class="btn btn-success" asp-controller="Serie" asp-action="Create">Nuevo Serie</a>
</p>

<!-- Simple Datatable start -->
<div class="card-box mb-30">
    <div class="pd-20">
        <h4 class="text-blue h4">Listado del Series</h4>
    </div>
    <div class="pb-20">
        <table id="example" class="data-table table stripe hover nowrap">
            <thead>
                <tr>
                    <th class="table-plus datatable-nosort">Tipo de Comprobante</th>
                    <th>Sucursal</th>
                    <th>Serie</th>
                    <th class="datatable-nosort">Accion</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    string selectedRow = "";
                    if (item.DocumentTypeBranchOfficeSerieId == (int?)ViewData["DocumentTypeBranchOfficeSerieId"])
                    {
                        selectedRow = "success";
                    }

    <tr class="@selectedRow">
        <td class="table-plus"> @Html.DisplayFor(modelItem => item.DocumentType)</td>
        <td>@Html.DisplayFor(modelItem => item.BranchOffice)</td>
        <td>@Html.DisplayFor(modelItem => item.Serie)</td>
        <td>
            <div class="dropdown2">
                <a class="btn btn-link font-24 p-0 line-height-1 no-arrow dropdown-toggle" href="#" role="button" data-toggle="dropdown">
                    <i class="dw dw-more"></i>
                </a>
                <div class="dropdown-menu dropdown-menu-right dropdown-menu-icon-list">
                    <a class="dropdown-item" asp-action="Edit" asp-route-id="@item.DocumentTypeBranchOfficeSerieId"><i class="dw dw-edit2"></i>Edit</a>
                </div>
            </div>
        </td>
    </tr>
}
            </tbody>
        </table>
    </div>
</div>
<!-- Simple Datatable End -->