@model List<SiGe.BranchOfficeModel>

@{ ViewData["Title"] = "Sucursales"; }

<div class="page-header">
    <div class="row">
        <div class="col-md-6 col-sm-12">
            <div class="title">
                <h4>Personal</h4>
            </div>
            <nav aria-label="breadcrumb" role="navigation">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="index.html">Sucursales</a></li>
                    <li class="breadcrumb-item active" aria-current="page">Lista del Sucursales</li>
                </ol>
            </nav>
        </div>
    </div>
</div>

<p>
    <a type="button" class="btn btn-success" asp-controller="BranchOffice" asp-action="Create">Nueva Sucursal</a>
</p>

<!-- Simple Datatable start -->
<div class="card-box mb-30">
    <div class="pd-20">
        <h4 class="text-blue h4">Listado del Sucursales</h4>
    </div>
    <div class="pb-20">
        <table id="example" class="data-table table stripe hover nowrap">
            <thead>
                <tr>
                    <th class="table-plus datatable-nosort">Codigo</th>
                    <th>Nombre</th>
                    <th>Direccion</th>
                    <th class="datatable-nosort">Accion</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    string selectedRow = "";
                    if (item.BranchOfficeId == (int?)ViewData["BranchOfficeId"])
                    {
                        selectedRow = "success";
                    }

    <tr class="@selectedRow">
        <td class="table-plus"> @Html.DisplayFor(modelItem => item.Code)</td>
        <td>@Html.DisplayFor(modelItem => item.Name)</td>
        <td>@Html.DisplayFor(modelItem => item.Address)</td>
        <td>
            <div class="dropdown2">
                <a class="btn btn-link font-24 p-0 line-height-1 no-arrow dropdown-toggle" href="#" role="button" data-toggle="dropdown">
                    <i class="dw dw-more"></i>
                </a>
                <div class="dropdown-menu dropdown-menu-right dropdown-menu-icon-list">
                    <a class="dropdown-item" asp-action="Edit" asp-route-id="@item.BranchOfficeId"><i class="dw dw-edit2"></i>Edit</a>
                    <a class="dropdown-item" asp-action="Delete" asp-route-id="@item.BranchOfficeId"><i class="dw dw-delete-3"></i>Eliminar</a>
                    <a class="dropdown-item" asp-action="Details" asp-route-id="@item.BranchOfficeId"><i class="dw dw-delete-3"></i>Detalles</a>
                </div>
            </div>
        </td>
    </tr>
}
            </tbody>
        </table>
    </div>
</div>
<!-- Simple Datatable End -->